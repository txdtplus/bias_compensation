function XG0 = Linear_RPC(rpc,point)
%caculate the initial value of linear RPC
%   rpc and point are classes
m = point.m;
l = zeros(2*m,1);
l(1:2:end-1) = point.r;
l(2:2:end) = point.c;

A() = point.
end

